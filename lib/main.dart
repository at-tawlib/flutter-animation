// 27-04-2023 Animation with Flutter
// The app starts with the cat in the box, with the box flaps shaking
// when box is clicked, the cat appears to come out of the box
import 'package:flutter/material.dart';
import 'package:flutter/rendering.dart';
import 'src/app.dart';

void main() {
  // to show container's outline/space
  //debugPaintSizeEnabled = true;

  runApp(App());
}
